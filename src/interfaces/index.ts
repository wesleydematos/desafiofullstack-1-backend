export interface ICustomerContactRequest {
  name: string;
  email: string;
  phoneNumber: string;
}

export interface ICustomerContactData {
  name: string;
  email: string;
  phoneNumber: string;
  register_date: string;
}
